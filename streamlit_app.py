import streamlit as st

# --- หน้าจอที่ 1: หน้าหลัก ---
def show_home():
    st.title("🏥 HIV Care Interactive Guide")
    st.write("### ยินดีต้อนรับสู่ระบบช่วยตัดสินใจการรักษา HIV")
    st.info("โปรดเลือกเมนูการใช้งานทางด้านซ้ายมือ (Sidebar)")
    st.image("https://images.unsplash.com/photo-1576091160399-112ba8d25d1d?auto=format&fit=crop&w=800&q=80", caption="Digital Health for Thailand")

# --- หน้าจอที่ 2: ผู้ใหญ่ (Adult) พร้อม Renal Dose ---
def show_adult():
    st.header("👤 ผู้ใหญ่ (Adult Regimen)")
    st.subheader("ระบบแนะนำสูตรยาและการปรับ Dose ตามค่าไต")
    
    # Input ข้อมูล
    col1, col2 = st.columns(2)
    with col1:
        egfr = st.number_input("ระบุค่า eGFR (mL/min)", min_value=0, value=90)
    with col2:
        cd4 = st.number_input("ระบุค่า CD4 (cells/mm3)", min_value=0, value=500)
    
    contraindication = st.multiselect(
        "ข้อห้ามใช้/ประวัติแพ้ยา",
        ["ไม่มี", "แพ้ Abacavir (HLA-B*5701 +ve)", "ไขมันสูง (Dyslipidemia)", "โรคจิตเวช (Psychiatric)"]
    )

    # ปุ่มประมวลผล
    if st.button("ประมวลผล (Analyze)"):
        st.divider()
        st.write("### 🩺 ผลการวิเคราะห์ (Analysis)")

        # 1. แนะนำสูตรยาหลัก (Regimen Selection)
        st.write("#### 1. สูตรยาต้านไวรัส (Regimen)")
        if egfr >= 30:
            if "แพ้ Abacavir (HLA-B*5701 +ve)" in contraindication:
                 st.success("**✅ สูตรแนะนำ: TDF + 3TC + DTG (TLD)**")
            else:
                 st.success("**✅ สูตรแนะนำ (Preferred): TDF + 3TC + DTG (TLD)**")
            st.caption("สูตรเม็ดรวม วันละ 1 เม็ด (หาก eGFR > 30)")
        else:
            st.warning("**⚠️ ค่า eGFR < 30: ควรหลีกเลี่ยง TDF**")
            st.info("**ทางเลือก (Alternative):**\n- **TAF/FTC + DTG** (ปลอดภัยกว่าในโรคไต)\n- **ABC/3TC + DTG** (ถ้าไม่แพ้ ABC)")

        # 2. การปรับขนาดยาตามไต (Renal Dosage Adjustment)
        st.divider()
        st.write(f"#### 2. การปรับขนาดยาตาม eGFR ({egfr} mL/min)")
        
        if egfr >= 50:
            st.success("✨ **ไม่ต้องปรับขนาดยา (No Adjustment Needed)**\n- ใช้ขนาดปกติได้ทุกตัว (Standard Dose)")
        else:
            st.error(f"🚨 **จำเป็นต้องปรับขนาดยา (Adjustment Required)** เนื่องจาก eGFR < 50")
            
            # สร้างตารางแนะนำ Dose
            st.markdown("##### คำแนะนำรายตัวยา (Individual Drug Dosing):")
            
            # Logic การปรับยา 3TC
            if 30 <= egfr < 50:
                st.write("- **3TC (Lamivudine):** ลดเหลือ **150 mg วันละ 1 ครั้ง**")
            elif egfr < 30:
                st.write("- **3TC (Lamivudine):** **150 mg วันแรก** หลังจากนั้น **50-100 mg วันละ 1 ครั้ง** (ขึ้นกับระดับไต)")

            # Logic การปรับยา TDF
            if egfr < 50:
                 st.write("- **TDF (Tenofovir DF):** ❌ **ไม่แนะนำให้ใช้** (หรือปรับเป็น 300 mg ทุก 48 ชม. ซึ่งปฏิบัติยาก)")
                 st.write("  👉 *แนะนำเปลี่ยนเป็น TAF หรือ ABC*")

            # Logic การปรับยา FTC
            if 30 <= egfr < 50:
                st.write("- **FTC (Emtricitabine):** **200 mg ทุก 48 ชั่วโมง**")
            elif egfr < 30:
                 st.write("- **FTC (Emtricitabine):** **200 mg ทุก 72-96 ชั่วโมง** (ตามความรุนแรง)")

# --- หน้าจอที่ 3: เด็ก (Pediatric) ---
def show_pediatric():
    st.header("👶 เด็ก (Pediatric)")
    st.warning("🚧 กำลังพัฒนา (Under Construction)")

# --- ส่วนควบคุมหลัก (Main Control) ---
def main():
    st.sidebar.title("เมนูหลัก")
    menu = st.sidebar.radio(
        "ไปที่หน้า:",
        ["🏠 หน้าหลัก", "👤 ผู้ใหญ่ (Adult)", "👶 เด็ก (Pediatrics)", "💊 ป้องกัน (PrEP/PEP)"]
    )

    if menu == "🏠 หน้าหลัก":
        show_home()
    elif menu == "👤 ผู้ใหญ่ (Adult)":
        show_adult()
    elif menu == "👶 เด็ก (Pediatrics)":
        show_pediatric()
    elif menu == "💊 ป้องกัน (PrEP/PEP)":
        st.header("💊 PrEP / PEP")
        st.info("รออัปเดตข้อมูล...")

if __name__ == "__main__":
    main()
